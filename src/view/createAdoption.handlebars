<div class="tooltips">
        <img src="./images/help.jpg" with="25" height="25" alt="Ajuda" />
        <span class="tooltiptext">Nesta p&aacute;gina poder&aacute; registar uma nova adopção no SI.</span>
    </div>
<div id="register">
    <h1>Registar adopção</h1>
    <form id="registerForm" onsubmit="createAdoption(); return false">
        <div class="form-group">
            <label>Adoptante</label>
            <select id="adopterSelect" class="input-form">
                {{#adopters}}
                <option value="{{adopter_id}}_{{adopter}}">{{adopter_id}} - {{adopter}}</option>
                {{/adopters}}
            </select>
            <span class="highlight"></span>
            <span class="bar"></span>
            <span class="text-danger"></span>
        </div>
        <div class="form-group">
            <label>Animal</label>
            <select id="animalSelect" class="input-form">
                {{#animals}}
                <option value="{{animal_id}}_{{animal}}">{{animal_id}} - {{animal}}</option>
                {{/animals}}
            </select>
            <span class="highlight"></span>
            <span class="bar"></span>
            <span class="text-danger"></span>
        </div>
        <div class="form-group">
            <label>Data de Adopção</label>
            <input id="adoptionDate" class="input-form" type="date" name="adoptionDate" value="{{adoption.adoptionDate}}" required />
            <span class="highlight"></span>
            <span class="bar"></span>
            <span class="text-danger"></span>
        </div>
        <div id="submit" class="form-group">
            <button type="submit" class="confirmationButton">Registar</button>
        </div>
    </form>
</div>