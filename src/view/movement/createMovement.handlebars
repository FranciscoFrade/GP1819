<div class="tooltips">
        <img src="./images/help.jpg" with="25" height="25" alt="Ajuda" />
        <span class="tooltiptext">Nesta p&aacute;gina poder&aacute; registar uma saída de um animal registado no SI.</span>
    </div>
<div id="register">
    <h1>Registar Saída</h1>
    <form id="registerForm" onsubmit="createMovement(); return false">
        <div class="form-group">
            <label>Responsável</label>
            <select id="userSelect" class="input-form">
                {{#users}}
                <option value="{{user_id}}_{{user}}">{{user_id}} - {{user}}</option>
                {{/users}}
            </select>
            <span class="highlight"></span>
            <span class="bar"></span>
            <span class="text-danger"></span>
        </div>
        <div class="form-group">
            <label>Animal</label>
            <select id="animalSelect" class="input-form">
                {{#animals}}
                <option value="{{animal_id}}_{{animal}}">{{animal_id}} - {{animal}}</option>
                {{/animals}}
            </select>
            <span class="highlight"></span>
            <span class="bar"></span>
            <span class="text-danger"></span>
        </div>
        <div id="submit" class="form-group">
            <button type="submit" class="confirmationButton">Registar</button>
        </div>
    </form>
</div>